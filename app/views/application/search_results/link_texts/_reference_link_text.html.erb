<%= reference.citation_html.try('html_safe') || '[No citation found.]' %>
<%= display_pages(reference.pages) %>
<% if reference.duplicate? %>
  <span class="badge badge-duplicate" title="Record flagged as duplicate">
    duplicate
  </span>
<% end %>
